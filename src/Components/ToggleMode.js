import React from "react";

function ToggleMode(){
    return (
      <div className="container-btn">
        <button className="toggle-btn"> <i class="fa-solid fa-toggle-on"></i> Light-mode </button>
      </div>
    );
}

export default ToggleMode;